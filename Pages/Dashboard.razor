@page "/dashboard"
@inject Statify.Services.SpotifyAuthService Auth

<h1>Dashboard</h1>

@if (Auth.IsLoggedIn)
{
    <p>Je bent ingelogd met Spotify ✅</p>
    <p>(De authorization code wordt niet meer op het scherm getoond.)</p>
}
else
{
    <p>Not logged in.</p>
}
